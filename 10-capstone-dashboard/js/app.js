/* =========================================================
   CAPSTONE — app.js
   This file is intentionally mostly empty. Each widget manages
   itself in its own IIFE-wrapped file. Only put things here that
   are truly shared across the whole dashboard (e.g. a global
   "last updated" timestamp, or a shared theme toggle) — resist
   the urge to let widgets reach into each other's state directly.
   That separation is the actual point of this project.
   ========================================================= */

// TODO (optional): a "last refreshed" timestamp shown in the header,
// or a light/dark mode toggle that applies a class to <body>.
