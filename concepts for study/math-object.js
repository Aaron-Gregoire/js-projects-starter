// Math = build-in object that provides a
//        collection of properties and methods

let x = 3;
let y = 2;
let z = 1;

// z.Math.round(x);
// z.Math.floor(x);      ALWAYS rounds down
// z.Math.ceil(x);       ALWAYS rounds up
// z.Math.trunc(x);      gets rid of decimal portion
// z.Math.pow(x, y);     exponent x to the power of y
// z.Math.sqrt(x);       square root of number
// z.Math.log(x);        logerithem of number
// z.Math.sin(x);    
// z.Math.cos(x);
// z.Math.tan(x);
// z.Math.abs(x);        absolute value of number gets rid of - sign
// z.Math.sign(x);       

// let randomNum = Math.random(); will create random number between 0 and 1
// let max = Math.max(x, y, z); max of the variables
// let min = Math.min(x, y, z);  min of the variables